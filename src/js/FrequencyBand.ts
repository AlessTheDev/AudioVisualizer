export default class FrequencyBand{
    startSample: number;
    endSample: number;

    constructor(startsample: number, endSample: number){
        this.startSample = startsample;
        this.endSample = endSample;
    }
}